<img src="../assets/images/burger.png" alt="pixelBurger">
<h1>Eat-Da-Burger!</h1>
<div class="columns">
    <div class="column">
        <h2>Burgers to eat:</h2>

        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            {{> burgers/devour devour=true}}
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="column">
        <h2>Burgers to exercise away:</h2>

        <ul>
            {{#each burgers}}
            {{#if devoured}}
            {{> burgers/devour devour=false}}
            {{/if}}
            {{/each}}
        </ul>
    </div>
</div>
<h2>Order a burger!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="sleepy">Did you already eat this burger?</label><br>
        <input type="radio" name="sleepy" value="1" checked> Yes!<br>
        <input type="radio" name="sleepy" value="0"> Not yet!
    </div>

    <button type="submit">Order Up!</button>
</form>